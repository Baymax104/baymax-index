## 1. About 组件

- [x] 1.1 新增 `AboutSection` 组件，包含线上 About 文案、信息块、兴趣标签和响应式内部布局
- [x] 1.2 复用 `sectionClass`，让 About 区块支持右侧进入动画

## 2. 首页编排

- [x] 2.1 更新 `App`，新增 About ref 与 `useInView` 可见性状态
- [x] 2.2 将内容区调整为线上骨架，并把 About 区块插入 GitHub 区块之前

## 3. 验证

- [x] 3.1 运行构建验证 TypeScript 与 Tailwind 类可用
- [x] 3.2 检查当前变更满足 `home-about-section` spec 中的文本、顺序和结构要求
