## 1. 背景组件

- [x] 1.1 新增 `ContentBackground` 组件，渲染静态点阵、短线和散点装饰
- [x] 1.2 确保组件使用 `aria-hidden` 与 `pointer-events-none`

## 2. 首页集成

- [x] 2.1 将 `ContentBackground` 插入 Hero 之后内容区的背景层
- [x] 2.2 保持 About、GitHub、留言板和 Footer 位于装饰层之上

## 3. 验证

- [x] 3.1 运行构建和 lint
- [x] 3.2 检查敏感 token 未进入 `src` 或 `dist`
- [x] 3.3 复核 OpenSpec 任务状态
