## 1. 类型与配置

- [x] 1.1 扩展 Live2D 消息类型，支持助手消息的 `thinking`、`typing` 和 `done` 状态。
- [x] 1.2 增加 mock 回复文案、思考延迟和打字间隔配置，并保持后续替换 API provider 的边界清晰。

## 2. 回复流程

- [x] 2.1 新增 mock reply provider 或等效函数，通过消息上下文返回 mock 回复文本。
- [x] 2.2 改造提交逻辑：非空提交后追加用户消息和助手占位消息。
- [x] 2.3 实现 `idle -> thinking -> typing -> idle` 回复状态机。
- [x] 2.4 实现打字机计时器，逐字更新当前助手消息并在完成后释放提交门禁。
- [x] 2.5 在组件卸载时清理 pending timers，避免卸载后继续更新状态。

## 3. 对话框 UI

- [x] 3.1 在消息列表中渲染助手思考状态。
- [x] 3.2 在助手打字阶段展示逐步输出的消息内容。
- [x] 3.3 回复进行中禁用提交入口，防止并发提交。
- [x] 3.4 确认对话框收起和展开不重置当前回复状态。

## 4. 验证

- [x] 4.1 使用 Playwright 验证桌面端提交后依次出现用户消息、思考状态、打字机回复和最终完整回复。
- [x] 4.2 使用 Playwright 验证回复过程中不能并发提交。
- [x] 4.3 使用 Playwright 验证回复过程中收起再展开后状态仍然连续。
- [x] 4.4 使用 Playwright 验证移动端不显示 Live2D 人物、对话框和回复 UI。
- [x] 4.5 运行项目 lint 或 build，确认变更没有引入类型或构建错误。
